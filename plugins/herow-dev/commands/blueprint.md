---
description: (herow) Complete gstack planning phase — persistence via hook, local `.claude/plans/<slug>/` (not committed)
argument-hint: <feature/change description>
effort: medium
---

You are in the **PURE PLANNING** phase. **DO NOT write production code** and **DO NOT create a worktree** — blueprint runs on the main tree and only generates a plan in the plan directory `.claude/plans/<slug>/` (local, gitignored folder) that references the artifacts generated by gstack skills. Worktree isolation (`.claude/worktree`, base = current branch) happens later, in `/herow-dev:execute`.

> **Layout (one directory per plan):** each plan lives in `.claude/plans/<slug>/`, containing `plan.md`, `state.json`, `source.md` (optional), and an `artifacts/` subfolder with **all** the data this plan's orchestration produces (research notes, subagent outputs, draft files). Nothing loose at the `.claude/plans/` root, never in `/tmp`.

Description:

**$ARGUMENTS**

## Initial setup (do this first, in order)

0. **Pull latest first (fail-open, automatic).** A `UserPromptSubmit` hook (`scripts/pull-latest.sh`) fast-forwards the current branch to its upstream (`git pull --ff-only`) on the main tree **before** this command runs, so planning always reflects the freshest code. It's fail-open (offline/detached/no-upstream/diverged all warn and continue, never block) and opt-out via `HEROW_SKIP_PULL=1`. You don't invoke it — just be aware the base is already fresh.
1. `mkdir -p .claude/plans` and sweep dead markers from old sessions (inert, but they accumulate): `find .claude/plans -maxdepth 1 -name '.active-*' -mtime +7 -delete 2>/dev/null || true`
2. **Ensure `.claude/plans/` is gitignored (idempotent).** Run the guard below — it appends `.claude/plans/` to git's global excludesfile (only if missing) and to the repo's `.gitignore` (only if missing), **keeping** the legacy `.plans/` line:

   ```bash
   GI="$(git config --global core.excludesfile)"; GI="${GI:-$HOME/.gitignore}"
   [ -f "$GI" ] || : > "$GI"
   grep -qxF '.claude/plans/' "$GI" || printf '%s\n' '.claude/plans/' >> "$GI"
   if [ -f .gitignore ] && ! grep -qxF '.claude/plans/' .gitignore; then
     printf '%s\n' '.claude/plans/' >> .gitignore
   fi
   ```
3. Generate the plan slug: `TS=$(date -u +%Y%m%d-%H%M%S)`, kebab-case slug from the description (max 40 chars, **only `[a-z0-9-]`** — the hook silently rejects any other character), `SLUG="${TS}-<slug>"`. This `SLUG` is the name of the plan's **directory**.
4. **Claim the plan directory as an atomic uniqueness claim** — `mkdir` **without** `-p`:

   ```bash
   until mkdir ".claude/plans/$SLUG" 2>/dev/null; do SLUG="${SLUG}-2"; done
   mkdir -p ".claude/plans/$SLUG/artifacts"
   ```
   `mkdir` is atomic at the filesystem level: if the directory already exists (same-second + same-slug collision across parallel sessions), the loop picks a new suffix. **Never** use `mkdir -p` for the claim.
5. **Activate the harness tracker (session-scoped marker):**

   ```bash
   echo "$SLUG" > ".claude/plans/.active-$CLAUDE_SESSION_ID"
   ```
   The marker is tied to **your** session (`$CLAUDE_SESSION_ID`), so parallel blueprints in the same repo **don't collide** and the hook never logs another session's skills against this plan. From here on, the herow-dev plugin's `PostToolUse:Skill` hook (`blueprint-track.sh`) automatically logs each executed skill to `.claude/plans/$SLUG/state.json`; artifacts are detected by mtime diffing **inside `.claude/plans/$SLUG/artifacts/`** — write any orchestration output you want tracked there. You **don't** need to take manual snapshots.
6. **Don't create `plan.md` now.** `plan.md` is written exactly once, at final consolidation — its absence marks a plan as still in progress, so an aborted blueprint is never resolved by `/herow-dev:execute`.

## Language of generated artifacts

All generated code, comments, commit messages, and documentation must be in English, even when the blueprint, questions, or user input are in Portuguese — unless the user explicitly requests otherwise.

## Mandatory skill sequence

Just run them in order — the hook handles persistence:

1. **`/office-hours`** — initial design doc
2. **`/plan-ceo-review`** — strategic challenge
3. **`/plan-eng-review`** — architecture, data, tests, edge cases
4. **If it touches UI:** `/plan-design-review`
5. **If it touches API/SDK/docs:** `/plan-devex-review`
6. **Optional:** `/spec` — refines into an executable spec

## Final consolidation

Order matters — the tracker (`.claude/plans/.active-$CLAUDE_SESSION_ID`) stays active until step 7, so that skills triggered in step 4 also get logged.

1. **Read state.json:** `cat .claude/plans/$SLUG/state.json` — contains the list of executed skills (`skills[].skill`, already normalized without namespace/leading slash: e.g. `office-hours`) and detected artifacts.
   - **If state.json doesn't exist** (hook not registered or failed): warn explicitly, build the "gstack Artifacts" section manually from the outputs you observed each skill produce, and mark as ✅ in the checklist below the steps you know ran in this session.
2. **Coverage checklist (always show to the user):** compare state.json against the canonical list and show each step with one of these states:
   - ✅ executed — present in state.json
   - ⬜ not executed — applicable, but didn't run
   - ➖ not applicable — condition doesn't apply (e.g. `/plan-design-review` without UI, `/plan-devex-review` without API/SDK/docs) — include the reason in 1 line

   Canonical list: `office-hours`, `plan-ceo-review`, `plan-eng-review`, `plan-design-review` (conditional on UI), `plan-devex-review` (conditional on API/SDK/docs), `spec` (optional).
3. **Offer to trigger the missing steps** — `AskUserQuestion` with `multiSelect: true`:
   - One option per step not executed (include the ➖ ones too, labeled "not applicable"), with a 1-line description of what the skill adds.
   - If only **one** step is missing, add a second option "None — finalize the plan" (the question requires ≥2 options).
   - If **zero** steps are missing, skip the question.
4. **Run the selected skills** (the tracker is still active, so they land in the same state.json) and **re-read state.json**.
5. Use the final artifact list to fill in the plan's "gstack Artifacts" section (paths + role of each). If there was source material, save it to `.claude/plans/$SLUG/source.md`.
6. **Write the plan** to `.claude/plans/$SLUG/plan.md` (once — this is what marks the plan as ready for `/herow-dev:execute`). There is **no** more `latest.txt`: `/herow-dev:execute` resolves the most recent plan by directory, and you print the exact command in the final report.
7. **Deactivate the tracker and clean up snapshots:** `rm -f ".claude/plans/.active-$CLAUDE_SESSION_ID"` and `rm -rf ".claude/plans/$SLUG/.snap"` (important — otherwise future skills in this session would keep being logged against this plan).

## Required format of `.claude/plans/$SLUG/plan.md`

```markdown
# PLAN: <short title>

> **Slug:** <SLUG>
> **Dir:** `.claude/plans/<SLUG>/`
> **Created:** <ISO>
> **State:** `.claude/plans/<SLUG>/state.json`
> **Target branch:** <type>/<slug> — `<type>` inferred from the objective, Conventional Commits: feat | fix | refactor | chore | docs (same rule as `/herow-dev:execute`)

## Objective
<1-2 sentences.>

## Scope
- Includes: <list>
- Does NOT include: <list>

## gstack Artifacts (MANDATORY READING before implementing)
The hook automatically detects **only** files written to `.claude/plans/<SLUG>/artifacts/` (logged in `state.json`). Outputs from gstack skills that go outside the plan directory (e.g. `~/.gstack/...`) are **not** detected — list them manually from what each skill generated in this session:

- `<path>` — generated by `/office-hours` — <role>
- `<path>` — generated by `/plan-ceo-review` — <role>
- ...

> These files are NOT in git (`.claude/plans/` and gstack output folders are usually local). If `/herow-dev:execute` runs on another machine, fall back to the summary below.

## Executable summary (fallback)
<3-5 bullets capturing the essence of the artifacts' decisions>

## Code files to touch
- `path/to/file1.ts` — <what changes>

## Implementation steps
1. **<step>** — <what to do>
   - **Verification:** <executable command>

## Global acceptance criteria
- [ ] <observable>

## Risks and decisions
- <risk> → <mitigation>

## Recommended subagent
<fullstack-developer | python-pro | ...>
```

## Final report

- Plan directory: `.claude/plans/<SLUG>/`
- Plan: `.claude/plans/<SLUG>/plan.md` · State: `.claude/plans/<SLUG>/state.json`
- gstack step coverage checklist (✅/⬜/➖, same as consolidation, already reflecting the extra steps triggered)
- Next command (print the exact path): `/herow-dev:execute .claude/plans/<SLUG>/` — isolates into `.claude/worktree/<slug>` based on the current branch
